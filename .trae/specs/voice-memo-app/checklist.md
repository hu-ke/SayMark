# Checklist

- [x] 后端工程结构已创建，FastAPI 可正常启动
- [x] `.env` 加载逻辑生效，`DASHSCOPE_API_KEY`、`MONGO_URI` 等必需变量缺失时启动报错
- [x] 后端启动时自动创建"未分类"顶级目录（已存在则跳过）
- [x] `folders` 与 `files` 集合字段与 spec 一致（仅文件存储内容，元数据完整）
- [x] 文件夹接口（树查询/创建/重命名/删除）行为符合 spec，删除时递归删除子树
- [x] 文件接口（查询/创建/重命名/删除/移动）行为符合 spec
- [x] `POST /api/notes` 能将转录文本转为 markdown 笔记并存入指定/默认目录
- [x] Qwen 调用封装正确，密钥来自环境变量，未硬编码
- [x] 笔记生成 Prompt 产出包含标题的 markdown，文件名取自标题
- [x] 指令解析 Prompt 能正确输出 JSON，覆盖 create_note/create_folder/rename/delete/move_file/locate_folder/list 动作
- [x] `POST /api/ai/command` 能自主执行解析后的指令并返回结果
- [x] 歧义/未找到场景返回可读错误信息
- [x] iOS 工程已配置麦克风与语音识别权限
- [x] iOS 语音识别（zh-CN）能将说话转为文本
- [x] iOS 目录树 UI 正确展示多级嵌套，支持进入/展开
- [x] iOS 支持文件/文件夹的重命名、删除、新建、移动操作
- [x] iOS 录音结束后笔记生成并刷新目录树
- [x] iOS 语音/文字指令入口能触发后端 AI 指令并按返回结果更新 UI（如定位文件夹）
- [x] 端到端验证：说一句话生成笔记并存入未分类
- [x] 端到端验证：语音/文字指令完成定位、移动、增删改查
- [x] 端到端验证：多级目录嵌套与递归删除正确
